import React from 'react';
import styles from './Header.less';

const Header = () => {
  return (
    <div className={styles.test}>
      Example
    </div>
  );
};

Header.propTypes = {
};

export default Header;
